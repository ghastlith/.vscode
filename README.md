# .vscode

This is a storage repository which contains all my vscode settings and it's purpose is to provide easy access on the fly.

The way to use it is to simply clone it inside the root of the repository I'm using at the moment. By doing this vscode will automatically fetch the config from the .vscode folder.
